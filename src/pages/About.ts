import { Component } from '../definitions/component.model';

const About: Component = {
  render: async () => /*html*/ `<section class="section"><h1>About</h1></section>`,
  afterRender: async () => {},
};

export default About;
